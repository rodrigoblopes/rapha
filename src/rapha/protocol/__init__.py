"""Projeto 60 Dias, extracted from the course material into structured data.

⚠️ The *parsers* live here. The *extracted content* does not — it is written to
`%RAPHA_HOME%\\protocol\\`, outside the repo, because the course is purchased
material and derived-from-copyrighted is still copyrighted (ADR-002).

A fresh clone therefore does nothing useful until `rapha extract` has run. That is
correct: the repo is the machine, not the material.
"""
